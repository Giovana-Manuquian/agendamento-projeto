import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Scheduling App",
    template: "%s | Scheduling App",
  },
  description: "Sistema de agendamento e gestão para unidades e profissionais.",
  applicationName: "Scheduling App",
  metadataBase: new URL("http://localhost:3001"),
  openGraph: {
    title: "Scheduling App",
    description: "Agende serviços e gerencie atendimentos com praticidade.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="pt-BR"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
